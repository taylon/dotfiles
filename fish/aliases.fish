# Navigation
alias dot 'cd $DOTFILES_PATH'
alias dev 'cd $DEV_PATH'
alias .. 'cd ..'
alias ... 'cd ../..'
alias .... 'cd ../../..'
alias ..... 'cd ../../../..'

# exa
alias ls 'exa -a'

# trash-cli
# alias rm trash

# Git
alias g git
alias s 'git status'

# bat
alias cat bat
