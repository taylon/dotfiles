# Navigation
alias dot 'cd $DOTFILES_PATH'
alias dev 'cd $DEV_PATH'
alias .. 'cd ..'
alias ... 'cd ../..'
alias .... 'cd ../../..'
alias ..... 'cd ../../../..'

# exa
alias ls 'exa -a'

# trash-cli
alias rm trash

# Git
alias g git

# Ag
alias ag 'ag -S --path-to-ignore ~/.agignore'

# bat
alias cat bat
