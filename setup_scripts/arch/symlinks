#!/usr/bin/env bash

# Systemd
ln -s $DOTFILES_PATH/systemd $HOME/.config/systemd

# Tilix
ln -s $DOTFILES_PATH/tilix $HOME/.config/tilix

# Xmodmap - Custom mouse and keyboard mapping
ln -s $DOTFILES_PATH/setup_scripts/arch/Xmodmap $HOME/.Xmodmap

